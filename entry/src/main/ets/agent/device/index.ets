/**
 * 设备模块导出
 */

export { DeviceService, ScreenshotResult, ExecuteResult } from './DeviceService';
export { UITestClient, ConnectionState, InitPhase, UINode, LayoutResult, TouchResult } from './UITestClient';
